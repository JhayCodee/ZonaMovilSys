@model List<Modelo.Ventas.ReporteClienteVM>

@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Reporte de Clientes por Periodo</title>
    <link rel="stylesheet" href="~/Content/bootstrap.min.css" />
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f8f9fa;
            color: #333;
            margin: 0;
            padding: 0;
        }

        .container {
            margin-top: 20px;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .header {
            text-align: center;
            margin-bottom: 20px;
        }

            .header h1 {
                margin-bottom: 10px;
                color: #007bff;
            }

            .header p {
                margin: 0;
                font-size: 16px;
            }

        .table {
            width: 100%;
            margin-bottom: 1rem;
            color: #212529;
            border-collapse: collapse;
        }

            .table th, .table td {
                padding: .75rem;
                vertical-align: top;
                border: 1px solid #dee2e6;
            }

            .table thead th {
                vertical-align: bottom;
                border-bottom: 2px solid #dee2e6;
                background-color: #007bff;
                color: #fff;
            }

            .table tbody + tbody {
                border-top: 2px solid #dee2e6;
            }

            .table th {
                background-color: #f8f9fa;
            }

            .table .total {
                font-weight: bold;
                text-align: right;
            }

        .header-logo {
            text-align: center;
            margin-bottom: 20px;
        }

            .header-logo img {
                max-width: 150px;
                height: auto;
            }
    </style>
</head>
<body>
    <div class="container">
        <div class="header-logo">
            <img src="~/Imagenes/logoblue.jpg" alt="Logo ZonaMovil">
        </div>
        <div class="header">
            <h1>Reporte de Clientes por Periodo</h1>
            <p>Desde: @ViewBag.FechaInicio.ToString("dd/MM/yyyy") Hasta: @ViewBag.FechaFin.ToString("dd/MM/yyyy")</p>
        </div>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Cliente</th>
                    <th>Correo</th>
                    <th>Teléfono</th>
                    <th>Departamento</th>
                    <th>Total de Compras</th>
                    <th>Monto Total Gastado ($)</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Cliente</td>
                        <td>@item.Correo</td>
                        <td>@item.Telefono</td>
                        <td>@item.Departamento</td>
                        <td>@item.TotalCompras</td>
                        <td>@item.MontoTotalGastado.ToString("C", new System.Globalization.CultureInfo("en-US"))</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</body>
</html>
